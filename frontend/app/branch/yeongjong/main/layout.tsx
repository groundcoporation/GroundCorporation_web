//메타데이터 위해서 생성

import type { Metadata } from "next";

// =====================================================================
// [영종점 전용 SEO(검색엔진 최적화) 및 카톡 공유 설정]
// 이 파일(layout.tsx)이 영종점 폴더 안에 있기 때문에,
// 사용자가 영종점 주소로 접속하면 메인 홈페이지의 간판은 무시되고 이 '영종점 전용 간판'이 최우선으로 걸립니다!
// =====================================================================
export const metadata: Metadata = {
  // 🟢 [1] 네이버, 구글 검색 결과용 설정
  // 누군가 "강인한슛팅스타 영종점"을 검색했을 때 네이버 검색 화면에 뜨는 정보입니다.
  title: "강인한슛팅스타 영종점", // 검색 결과에서 파란색으로 크고 굵게 뜨는 제목
  description: "강인한슛팅스타 영종점입니다. 쾌적한 환경에서 프리미엄 축구를 즐겨보세요.", // 파란 제목 아래에 뜨는 회색 요약 설명글

  // 🟡 [2] 카카오톡, 페이스북 등 링크 공유용 설정 (오픈 그래프)
  // 영종점 주소를 복사해서 카톡방에 보냈을 때 만들어지는 '미리보기 카드' 정보입니다.
  openGraph: {
    title: "강인한슛팅스타 영종점", // 카톡 미리보기 카드의 굵은 제목
    description: "강인한슛팅스타 영종점입니다. 쾌적한 환경에서 프리미엄 축구를 즐겨보세요.", // 카톡 미리보기 카드의 회색 설명글
    // ⚠️ 영종점만의 실제 구장 사진이 있다면 아래 주소를 그 사진 경로로 바꿔주세요! (현재는 회사 기본 로고)
    images: ["https://www.groundcorporation.com/resource/image/logo.png"],
  },
};

// =====================================================================
// 아래 코드는 page.tsx의 내용(화면)을 그대로 감싸서 보여주기 위한 껍데기 역할만 합니다.
// 절대 지우거나 건드리지 마세요! (애니메이션 등 기존 로직이 100% 그대로 유지됩니다)
// =====================================================================
export default function YeongjongMainLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}